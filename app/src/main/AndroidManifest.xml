<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.temabet">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/LightAppTheme">
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

        <service
            android:name="com.kirichripper.entertainment.temabet.service.Data"
            android:enabled="true"
            android:exported="true">
        </service>

        <activity android:name="com.kirichripper.entertainment.temabet.activity.History" />
        <activity android:name="com.kirichripper.entertainment.temabet.activity.BetDetails" />
        <activity android:name="com.kirichripper.entertainment.temabet.activity.Runs" />
        <activity android:name="com.kirichripper.entertainment.temabet.activity.Login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>